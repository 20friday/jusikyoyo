# 문의하기 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 로그인 회원이 서비스 문의·콘텐츠 제안을 보낼 수 있는 문의하기 기능을 만든다. 제출 시 Supabase에 저장되고 Resend로 운영자에게 이메일이 발송된다.

**Architecture:** `/contact` 폼 → `POST /api/contact` 엔드포인트 → Supabase `inquiries` 테이블 저장 + Resend 이메일 발송. 어드민 `/admin/contacts` 에서 목록 확인.

**Tech Stack:** Astro (SSR), Supabase (DB + RLS), Resend (이메일), TypeScript

---

## 파일 구조

| 파일 | 역할 |
|---|---|
| `src/pages/contact/index.astro` | 문의 폼 페이지 (새로 생성) |
| `src/pages/api/contact.ts` | POST 엔드포인트 (새로 생성) |
| `src/pages/admin/contacts.astro` | 어드민 문의 목록 (새로 생성) |
| `src/layouts/AdminLayout.astro` | 사이드바에 "문의 관리" 추가 (수정) |

> **참고:** `src/components/SiteFooter.astro` 에는 이미 `/contact` 링크가 있어서 수정 불필요.

---

### Task 1: Supabase `inquiries` 테이블 생성

**Files:**
- 없음 (Supabase 대시보드에서 SQL 실행)

- [ ] **Step 1: Supabase 대시보드에서 SQL 실행**

Supabase 대시보드 → SQL Editor에서 아래 SQL을 실행한다.

```sql
create table inquiries (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users(id),
  email text not null,
  type text not null check (type in ('service', 'content')),
  title text not null,
  content text not null,
  created_at timestamptz default now()
);

alter table inquiries enable row level security;

create policy "users can insert own inquiries"
  on inquiries for insert
  to authenticated
  with check (auth.uid() = user_id);
```

- [ ] **Step 2: 테이블 생성 확인**

Supabase 대시보드 → Table Editor에서 `inquiries` 테이블이 보이는지 확인한다.

---

### Task 2: Resend 설치 및 환경 변수 설정

**Files:**
- Modify: `.env` (RESEND_API_KEY 추가)
- Modify: `package.json` (resend 패키지 추가)

- [ ] **Step 1: Resend 계정 및 API 키 발급**

[resend.com](https://resend.com) 에서 계정 생성 후 API Keys 메뉴에서 키를 발급한다.

- [ ] **Step 2: resend 패키지 설치**

```bash
npm install resend
```

Expected output: `added 1 package`

- [ ] **Step 3: .env에 API 키 추가**

`.env` 파일에 아래 줄을 추가한다.

```
RESEND_API_KEY=re_xxxxxxxxxxxxxxxx
```

- [ ] **Step 4: Vercel 환경 변수에도 추가**

```bash
vercel env add RESEND_API_KEY
```

프롬프트에서 값 입력, 환경(Production/Preview/Development) 모두 선택.

- [ ] **Step 5: 커밋**

```bash
git add package.json package-lock.json
git commit -m "feat: install resend for email notifications"
```

---

### Task 3: `POST /api/contact` 엔드포인트

**Files:**
- Create: `src/pages/api/contact.ts`

- [ ] **Step 1: 파일 생성**

`src/pages/api/contact.ts` 를 아래 내용으로 만든다.

```typescript
import type { APIRoute } from 'astro';
import { Resend } from 'resend';

const TYPE_LABELS: Record<string, string> = {
  service: '서비스 문의',
  content: '콘텐츠 제안',
};

export const POST: APIRoute = async ({ locals, request }) => {
  // 1. 인증 확인
  if (!locals.session || !locals.user) {
    return new Response('Unauthorized', { status: 401 });
  }

  // 2. 입력값 파싱 및 검증
  let body: { type?: string; title?: string; content?: string };
  try {
    body = await request.json();
  } catch {
    return new Response('Invalid JSON', { status: 400 });
  }

  const { type, title, content } = body;
  if (!type || !['service', 'content'].includes(type)) {
    return new Response('type은 service 또는 content여야 해요', { status: 400 });
  }
  if (!title?.trim()) {
    return new Response('제목을 입력해주세요', { status: 400 });
  }
  if (!content?.trim()) {
    return new Response('내용을 입력해주세요', { status: 400 });
  }

  const email = locals.user.email ?? '';
  const userId = locals.user.id;

  // 3. Supabase 저장
  const { error: dbError } = await locals.supabase.from('inquiries').insert({
    user_id: userId,
    email,
    type,
    title: title.trim(),
    content: content.trim(),
  });

  if (dbError) {
    console.error('inquiries insert error:', dbError.message);
    return new Response('저장 중 오류가 발생했어요', { status: 500 });
  }

  // 4. Resend 이메일 발송 (실패해도 사용자에겐 성공으로 표시)
  try {
    const resend = new Resend(import.meta.env.RESEND_API_KEY);
    const typeLabel = TYPE_LABELS[type] ?? type;
    const submittedAt = new Date().toLocaleString('ko-KR', { timeZone: 'Asia/Seoul' });

    await resend.emails.send({
      from: 'TED PICK <onboarding@resend.dev>',
      to: ['20friday@gmail.com'],
      subject: `[테드픽 문의] ${typeLabel} — ${title.trim()}`,
      html: `
        <p><strong>유형:</strong> ${typeLabel}</p>
        <p><strong>보낸 사람:</strong> ${email}</p>
        <p><strong>제목:</strong> ${title.trim()}</p>
        <hr />
        <p style="white-space:pre-wrap;">${content.trim()}</p>
        <hr />
        <p style="color:#888;font-size:12px;">접수 시각: ${submittedAt}</p>
      `,
    });
  } catch (emailErr) {
    console.error('resend error:', emailErr);
    // 이메일 실패는 사용자에게 노출하지 않음
  }

  return new Response(JSON.stringify({ ok: true }), {
    status: 200,
    headers: { 'Content-Type': 'application/json' },
  });
};
```

- [ ] **Step 2: 커밋**

```bash
git add src/pages/api/contact.ts
git commit -m "feat: add POST /api/contact endpoint"
```

---

### Task 4: `/contact` 문의 폼 페이지

**Files:**
- Create: `src/pages/contact/index.astro`

- [ ] **Step 1: 파일 생성**

`src/pages/contact/index.astro` 를 아래 내용으로 만든다.

```astro
---
import Base from '../../layouts/Base.astro';

if (!Astro.locals.session) {
  return Astro.redirect('/login?next=/contact');
}

const email = Astro.locals.user?.email ?? '';
---

<Base title="문의하기 — 테드픽">
  <div class="contact-wrap">
    <div class="contact-header">
      <h1 class="contact-title">문의하기</h1>
      <p class="contact-sub">궁금한 점이나 의견을 남겨주세요. 확인 후 이메일로 답변드려요.</p>
    </div>

    <div id="form-area">
      <form id="contact-form" class="contact-form">
        <div class="form-field">
          <label class="form-label">문의 유형</label>
          <select class="form-select" name="type" required>
            <option value="service">서비스 문의</option>
            <option value="content">콘텐츠 제안</option>
          </select>
        </div>
        <div class="form-field">
          <label class="form-label">이메일</label>
          <input class="form-input" type="text" value={email} readonly />
        </div>
        <div class="form-field">
          <label class="form-label">제목 <span class="required">*</span></label>
          <input class="form-input" type="text" name="title" placeholder="문의 제목을 입력해주세요" required />
        </div>
        <div class="form-field">
          <label class="form-label">내용 <span class="required">*</span></label>
          <textarea class="form-textarea" name="content" rows="6" placeholder="문의 내용을 자세히 적어주세요" required></textarea>
        </div>
        <div id="error-msg" class="form-error" style="display:none;"></div>
        <button type="submit" id="submit-btn" class="btn-submit">문의 보내기</button>
      </form>
    </div>

    <div id="done-area" style="display:none;" class="done-area">
      <div class="done-icon">✉️</div>
      <p class="done-title">문의가 접수됐어요</p>
      <p class="done-sub">이메일로 답변드릴게요.</p>
      <a href="/" class="btn-home">홈으로 →</a>
    </div>
  </div>
</Base>

<script>
  const form = document.getElementById('contact-form') as HTMLFormElement;
  const btn = document.getElementById('submit-btn') as HTMLButtonElement;
  const errorMsg = document.getElementById('error-msg') as HTMLDivElement;
  const formArea = document.getElementById('form-area') as HTMLDivElement;
  const doneArea = document.getElementById('done-area') as HTMLDivElement;

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    btn.textContent = '전송 중...';
    btn.disabled = true;
    errorMsg.style.display = 'none';

    const data = Object.fromEntries(new FormData(form));

    const res = await fetch('/api/contact', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data),
    });

    if (res.ok) {
      formArea.style.display = 'none';
      doneArea.style.display = 'block';
    } else {
      const msg = await res.text();
      errorMsg.textContent = msg || '오류가 발생했어요. 다시 시도해주세요.';
      errorMsg.style.display = 'block';
      btn.textContent = '문의 보내기';
      btn.disabled = false;
    }
  });
</script>

<style>
  .contact-wrap { max-width: 560px; margin: 0 auto; padding: 40px 24px 80px; }
  .contact-header { margin-bottom: 28px; }
  .contact-title { font-size: 24px; font-weight: 800; color: var(--text-1); margin: 0 0 6px; }
  .contact-sub { font-size: 14px; color: var(--text-3); margin: 0; line-height: 1.6; }
  .contact-form { background: var(--card); border: 1px solid var(--border); border-radius: 14px; padding: 28px; display: flex; flex-direction: column; gap: 18px; }
  .form-field { display: flex; flex-direction: column; gap: 6px; }
  .form-label { font-size: 13px; font-weight: 700; color: var(--text-2); }
  .required { color: #ef4444; }
  .form-input { height: 42px; border-radius: 8px; border: 1.5px solid var(--border); padding: 0 12px; font-size: 14px; color: var(--text-1); background: var(--card); outline: none; }
  .form-input[readonly] { background: var(--bg); color: var(--text-3); cursor: default; }
  .form-select { height: 42px; border-radius: 8px; border: 1.5px solid var(--border); padding: 0 12px; font-size: 14px; color: var(--text-1); background: var(--card); outline: none; cursor: pointer; }
  .form-select:focus, .form-input:focus { border-color: #ed781f; }
  .form-textarea { border-radius: 8px; border: 1.5px solid var(--border); padding: 10px 12px; font-size: 14px; color: var(--text-1); background: var(--card); outline: none; resize: vertical; line-height: 1.65; font-family: inherit; }
  .form-textarea:focus { border-color: #ed781f; }
  .form-error { background: #fee2e2; color: #dc2626; border-radius: 8px; padding: 10px 14px; font-size: 13px; }
  .btn-submit { height: 46px; background: #ed781f; color: #fff; border: none; border-radius: 10px; font-size: 15px; font-weight: 700; cursor: pointer; transition: opacity 120ms; }
  .btn-submit:hover { opacity: 0.88; }
  .btn-submit:disabled { opacity: 0.5; cursor: not-allowed; }
  .done-area { text-align: center; padding: 60px 0; }
  .done-icon { font-size: 48px; margin-bottom: 16px; }
  .done-title { font-size: 20px; font-weight: 800; color: var(--text-1); margin: 0 0 8px; }
  .done-sub { font-size: 14px; color: var(--text-3); margin: 0 0 24px; }
  .btn-home { display: inline-block; padding: 10px 20px; background: #111; color: #fff; border-radius: 8px; font-size: 14px; font-weight: 700; text-decoration: none; }
</style>
```

- [ ] **Step 2: 개발 서버에서 확인**

```bash
npm run dev
```

브라우저에서 `http://localhost:4321/contact` 접속:
- 로그인 상태에서 폼이 보이는지 확인
- 로그아웃 상태에서 `/login?next=/contact` 로 리다이렉트되는지 확인

- [ ] **Step 3: 커밋**

```bash
git add src/pages/contact/index.astro
git commit -m "feat: add /contact inquiry form page"
```

---

### Task 5: 어드민 문의 목록 페이지

**Files:**
- Create: `src/pages/admin/contacts.astro`

- [ ] **Step 1: 파일 생성**

`src/pages/admin/contacts.astro` 를 아래 내용으로 만든다.

```astro
---
import AdminLayout from '../../layouts/AdminLayout.astro';
import { isAdmin } from '../../lib/adminGuard';
import { createAdminClient } from '../../lib/adminSupabase';

if (!Astro.locals.session) return Astro.redirect('/admin/login');
if (!isAdmin(Astro.locals.user?.email)) return Astro.redirect('/');

const adminDb = createAdminClient();
const { data: inquiries } = await adminDb
  .from('inquiries')
  .select('id, email, type, title, content, created_at')
  .order('created_at', { ascending: false })
  .limit(200);

const TYPE_LABELS: Record<string, string> = {
  service: '서비스 문의',
  content: '콘텐츠 제안',
};

function formatDate(dateStr: string) {
  const d = new Date(dateStr);
  return `${d.getFullYear()}.${String(d.getMonth()+1).padStart(2,'0')}.${String(d.getDate()).padStart(2,'0')} ${String(d.getHours()).padStart(2,'0')}:${String(d.getMinutes()).padStart(2,'0')}`;
}
---

<AdminLayout title="문의 관리">
  <div class="list-header">
    <div class="table-meta">총 {inquiries?.length ?? 0}건</div>
  </div>
  <div class="table-wrap">
    <table class="admin-table">
      <thead>
        <tr>
          <th>유형</th>
          <th>제목</th>
          <th>이메일</th>
          <th>접수일</th>
        </tr>
      </thead>
      <tbody>
        {inquiries?.map(q => (
          <tr class="inquiry-row" data-content={q.content} data-title={q.title} data-email={q.email} data-type={TYPE_LABELS[q.type] ?? q.type} data-date={formatDate(q.created_at)}>
            <td>
              <span class={`badge-type badge-${q.type}`}>{TYPE_LABELS[q.type] ?? q.type}</span>
            </td>
            <td class="td-title">{q.title}</td>
            <td class="td-gray">{q.email}</td>
            <td class="td-gray">{formatDate(q.created_at)}</td>
          </tr>
        ))}
        {(!inquiries || inquiries.length === 0) && (
          <tr><td colspan="4" class="td-empty">접수된 문의가 없어요</td></tr>
        )}
      </tbody>
    </table>
  </div>

  <!-- 상세 모달 -->
  <div id="modal-backdrop" class="modal-backdrop" style="display:none;">
    <div class="modal">
      <div class="modal-header">
        <div>
          <span id="modal-type" class="modal-badge"></span>
          <h2 id="modal-title" class="modal-title"></h2>
        </div>
        <button id="modal-close" class="modal-close">✕</button>
      </div>
      <div class="modal-meta">
        <span id="modal-email"></span>
        <span>·</span>
        <span id="modal-date"></span>
      </div>
      <p id="modal-content" class="modal-content"></p>
    </div>
  </div>
</AdminLayout>

<script>
  const rows = document.querySelectorAll('.inquiry-row');
  const backdrop = document.getElementById('modal-backdrop')!;
  const closeBtn = document.getElementById('modal-close')!;

  rows.forEach(row => {
    row.addEventListener('click', () => {
      const el = row as HTMLElement;
      document.getElementById('modal-title')!.textContent = el.dataset.title ?? '';
      document.getElementById('modal-type')!.textContent = el.dataset.type ?? '';
      document.getElementById('modal-email')!.textContent = el.dataset.email ?? '';
      document.getElementById('modal-date')!.textContent = el.dataset.date ?? '';
      document.getElementById('modal-content')!.textContent = el.dataset.content ?? '';
      backdrop.style.display = 'flex';
    });
  });

  closeBtn.addEventListener('click', () => { backdrop.style.display = 'none'; });
  backdrop.addEventListener('click', (e) => { if (e.target === backdrop) backdrop.style.display = 'none'; });
</script>

<style>
  .list-header { margin-bottom: 12px; }
  .table-meta { font-size: 13px; color: #888; }
  .table-wrap { background: #fff; border-radius: 12px; border: 1px solid #e8eaed; overflow: hidden; }
  .admin-table { width: 100%; border-collapse: collapse; font-size: 14px; }
  .admin-table th { background: #f9fafb; text-align: left; padding: 11px 16px; font-size: 12px; font-weight: 700; color: #888; border-bottom: 1px solid #e8eaed; }
  .admin-table td { padding: 10px 16px; border-bottom: 1px solid #f0f1f3; color: #333; vertical-align: middle; }
  .admin-table tr:last-child td { border-bottom: none; }
  .inquiry-row { cursor: pointer; }
  .inquiry-row:hover td { background: #fafafa; }
  .td-title { font-weight: 600; }
  .td-gray { color: #999; font-size: 13px; white-space: nowrap; }
  .td-empty { text-align: center; color: #bbb; padding: 40px; }
  .badge-type { font-size: 11px; font-weight: 700; border-radius: 5px; padding: 2px 7px; background: #f1f5f9; color: #64748b; }
  .badge-service { background: #eff6ff; color: #3b82f6; }
  .badge-content { background: #f0fdf4; color: #16a34a; }
  .modal-backdrop { position: fixed; inset: 0; background: rgba(0,0,0,0.4); display: flex; align-items: center; justify-content: center; z-index: 1000; padding: 24px; }
  .modal { background: #fff; border-radius: 14px; width: 100%; max-width: 560px; padding: 28px; display: flex; flex-direction: column; gap: 12px; }
  .modal-header { display: flex; justify-content: space-between; align-items: flex-start; gap: 12px; }
  .modal-badge { font-size: 11px; font-weight: 700; border-radius: 5px; padding: 2px 7px; background: #eff6ff; color: #3b82f6; display: inline-block; margin-bottom: 6px; }
  .modal-title { font-size: 17px; font-weight: 700; color: #111; margin: 0; }
  .modal-close { background: none; border: none; font-size: 18px; color: #999; cursor: pointer; flex-shrink: 0; padding: 0; }
  .modal-meta { font-size: 13px; color: #999; display: flex; gap: 8px; }
  .modal-content { font-size: 14px; color: #333; line-height: 1.8; white-space: pre-wrap; background: #f9fafb; border-radius: 8px; padding: 16px; margin: 0; }
</style>
```

- [ ] **Step 2: 커밋**

```bash
git add src/pages/admin/contacts.astro
git commit -m "feat: add /admin/contacts inquiry list page"
```

---

### Task 6: 어드민 사이드바에 "문의 관리" 추가

**Files:**
- Modify: `src/layouts/AdminLayout.astro`

- [ ] **Step 1: nav 배열에 항목 추가**

`src/layouts/AdminLayout.astro` 에서 `nav` 배열을 찾는다. `'회원 목록'` 항목 바로 위 `'회원'` 그룹 앞에 아래 항목을 추가한다.

변경 전:
```typescript
  { label: 'FAQ', href: '/admin/faq', icon: 'help' },
  { label: '구분', type: 'divider' },
  { label: '회원', type: 'group' },
```

변경 후:
```typescript
  { label: 'FAQ', href: '/admin/faq', icon: 'help' },
  { label: '문의 관리', href: '/admin/contacts', icon: 'mail' },
  { label: '구분', type: 'divider' },
  { label: '회원', type: 'group' },
```

- [ ] **Step 2: mail 아이콘 SVG 추가**

같은 파일에서 아이콘 렌더링 조건문 블록을 찾아 `mail` 케이스를 추가한다. 기존 `help` 아이콘 블록 바로 뒤에 추가한다.

```astro
{item.icon === 'mail' && (
  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <rect x="2" y="4" width="20" height="16" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>
  </svg>
)}
```

- [ ] **Step 3: 브라우저에서 확인**

`http://localhost:4321/admin/contacts` 에 접속해 사이드바에 "문의 관리" 메뉴가 보이는지 확인한다.

- [ ] **Step 4: 커밋**

```bash
git add src/layouts/AdminLayout.astro
git commit -m "feat: add contacts menu to admin sidebar"
```

---

### Task 7: 전체 흐름 최종 확인

- [ ] **Step 1: 문의 제출 테스트**

1. `http://localhost:4321/contact` 접속 (로그인 상태)
2. 유형·제목·내용 입력 후 "문의 보내기" 클릭
3. "문의가 접수됐어요" 완료 메시지가 표시되는지 확인

- [ ] **Step 2: DB 저장 확인**

Supabase 대시보드 → Table Editor → `inquiries` 테이블에 방금 제출한 데이터가 있는지 확인한다.

- [ ] **Step 3: 이메일 수신 확인**

`20friday@gmail.com` 받은 편지함에서 `[테드픽 문의]` 메일이 도착했는지 확인한다.

- [ ] **Step 4: 어드민 목록 확인**

`http://localhost:4321/admin/contacts` 에서 문의 목록이 보이고, 행 클릭 시 모달이 뜨는지 확인한다.

- [ ] **Step 5: 미로그인 리다이렉트 확인**

로그아웃 후 `http://localhost:4321/contact` 접속 → `/login?next=/contact` 로 리다이렉트되는지 확인한다.

- [ ] **Step 6: 최종 커밋 및 배포**

```bash
git push origin main
```
